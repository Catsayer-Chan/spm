---
appName:
workDir:
procfile:
env:
    PATH: /usr/local/bin:$PATH

processes:
    web:
        root:
        pidRoot:
        logRoot:
        # Supported signal: TERM QUIT INT
        stopSignal: TERM
        env:
            PORT: 3000
